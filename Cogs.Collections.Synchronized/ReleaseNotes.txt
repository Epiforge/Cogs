Added ProcessingQueue and AsyncProcessingQueue, which perform an action (synchronous or asynchronous, respectively) on each item enqueued in serial on a worker pool thread. Enqueueing items does not block.